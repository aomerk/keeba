# Built binaries (top-level only — don't shadow cmd/keeba/)
/keeba
/bin/
*.exe
*.exe~

# Test artifacts
*.test
*.out
coverage.txt
coverage.html

# IDE
.idea/
.vscode/
*.swp
.DS_Store

# Local env
.env
.env.*
!.env.example

# Build/cache
/bin/
/dist/

# keeba runtime artifacts
.keeba-cache/
*.kbdb
.keeba/

# Local-only A/B bench harness + prompt fixtures. Prompts are real
# investigation transcripts (Slack threads etc.) — never committed.
# Script is local because it embeds Claude Code session assumptions
# (auth flow, model, effort).
/scripts/bench-keeba-ab.sh
/bench-prompts/
