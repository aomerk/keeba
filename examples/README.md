# keeba examples

Worked configurations land here as `keeba init` and `keeba bench` mature.

Planned for v0.1 step 9 (per the build sequence): a clone of Karpathy's
`llm.c` initialised through keeba, with the bench-numbers checked in so the
README can link to a real ratio.

This dir is intentionally empty until that lands.
